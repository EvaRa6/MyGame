using UnityEngine;

public class PlayerInteractionManager : MonoBehaviour
{
    //HANDLE pick up item interaction (adds item inventory)

    //HANDLE open door item interaction (plays opening door animation)
}
